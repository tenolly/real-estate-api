# real-estate-api

# launch
```bash
docker-compose up --build
```

# full clean
```bash
docker system prune --all --volumes
```

# tests
```bash
pytest
```

# tests (docker)
```bash
docker-compose up test --build
```
